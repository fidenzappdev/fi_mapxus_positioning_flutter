# Example app specific ProGuard rules
# The Mapxus positioning plugin automatically includes its required ProGuard rules
# via consumer-rules.pro, so no plugin-specific rules are needed here.

# Add any example app specific rules below if needed
# For example:
# -keep class com.fidenz.mapxus_positioning_flutter_example.** { *; }

